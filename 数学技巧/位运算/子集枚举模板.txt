模板1:
int s = m;
while (s > 0) 
{
  // s 是 m 的一个非空子集
  s = (s - 1) & m;
}

模板2:
for (int s = m; s; s = (s - 1) & m)
// s 是 m 的一个非空子集

模板3:
for (int s = m;; s = (s - 1) & m) 
{
  // s 是 m 的一个子集
  if (s == 0) break;
}

注意:1,三个模板均是子集降序遍历
     2,模板1与2无法处理子集0,模板3可以