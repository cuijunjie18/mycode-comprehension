1,点差分:将叶子看成数组起点,根节点看出终点,易有叶子到根的路径为一条链,类比一维差分数组即可
2,边差分:直接将边进行差分较难,可以把对应的边用其下面的点权进行对应,便转换为了点差分

易错:1,单线程处理路径时,点差分为d[x]++; d[y]++; d[lca]--;d[f[lca][0]]--;
                     边差分为d[x]++; d[y]++; d[lca] -= 2;
    2,连续线程的时候,即路径行走连续,如1 -> 5 -> 7 -> 9这样
                   点差分对于除首尾外的每个点多进行了一次 +1操作,故要后续中减去(!!!注意细节,不能在差分数组中执行减操作,因为仅是单点多了,不能让区间一起减,
				   故应该先进行差分求和再对差分求和后的数组进行单点修改!)
                   边差分则不用,自己想
           
上面区别产生的原因是点有公共部分,而边没有